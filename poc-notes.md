Placeholder file for the browser-agent POC.

The pull request description is the real subject of this test: an image gets attached to it through the web UI.

This line exists so the pull request carries a diff against main.
